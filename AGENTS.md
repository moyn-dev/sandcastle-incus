# Agent Instructions

See [CLAUDE.md](CLAUDE.md).

## Compatibility rule

Features must be additive unless the user explicitly authorizes a change to
existing behaviour. Preserve established commands, topology, DNS records, and
defaults; introduce an opt-in interface for new behaviour.
